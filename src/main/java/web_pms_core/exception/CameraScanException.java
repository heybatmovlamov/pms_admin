package web_pms_core.exception;

public class CameraScanException extends CommonException {

    public CameraScanException() {
        super("CAMERA SCAN ERROR (LENGTH OR PATTERN)", "CAMERA SCAN ERROR (LENGTH OR PATTERN)");
    }
}
